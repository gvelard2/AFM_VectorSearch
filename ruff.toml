line-length = 120

exclude = [
    "notebooks/",
    ".git",
    "__pycache__",
]

[lint]
select = ["E", "F", "W"]
ignore = []

[lint.per-file-ignores]
"ui/app.py" = ["E402"]   # sys.path must be set before imports

[format]
quote-style = "double"
